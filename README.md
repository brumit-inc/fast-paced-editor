# Fast Paced Editor

A simple notepad application built with Electron.

## Features

- Create, open, and save text files
- Keyboard shortcuts (Ctrl+N, Ctrl+O, Ctrl+S)
- Line and column position tracking
- Modified file indicator
- Dark mode support
- Cross-platform (Windows, macOS, Linux)

## Installation

1. Install dependencies:
   ```bash
   npm install
   ```

2. Run the application:
   ```bash
   npm start
   ```

## Building

To create a distributable package:

```bash
npm run build
```

## Keyboard Shortcuts

- `Ctrl+N` / `Cmd+N` - New file
- `Ctrl+O` / `Cmd+O` - Open file
- `Ctrl+S` / `Cmd+S` - Save file
- `Ctrl+Shift+S` / `Cmd+Shift+S` - Save as
- `Ctrl+Q` / `Cmd+Q` - Quit

## License

MIT
```

---

## Installation Instructions

1. Create a new directory and navigate to it:
   ```bash
   mkdir notepad-electron
   cd notepad-electron
   ```

2. Copy all the files above into their respective locations

3. Install dependencies:
   ```bash
   npm install
   ```

4. Run the application:
   ```bash
   npm start
   ```

## Features Included

✅ New, Open, Save, Save As functionality
✅ Keyboard shortcuts
✅ Line and column position tracking
✅ Modified file indicator
✅ Menu bar with File, Edit, and View menus
✅ Dark mode support
✅ Status bar
✅ Cross-platform compatibility
✅ Secure IPC communication

This is a fully functional notepad application ready to run!